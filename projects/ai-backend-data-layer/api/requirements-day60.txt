# Day60 — Outbox, Redis/Celery Broker and Worker Recovery Integration.
# Python 3.10+ REQUIRED (existing ORM uses PEP 604 `X | Y` unions).
#
# Two clearly separated scopes:
#
# 1) EXECUTED_LOCAL_RUNTIME unit tests (no DB, no broker, no Docker) — needs only pytest.
#    `py_compile` does not execute imports, so it works without the runtime stack:
#    (strip the leading "# " from each line below and run it as-is)
#
#    cd projects/ai-backend-data-layer/api
#    python3 -m pip install pytest==7.4.3
#    python3 -m py_compile day60_delivery_recovery_logic.py day60_delivery_runtime.py day60_celery_app.py day60_relay.py day60_sweeper.py day60_runtime_app.py day60_celery_config.py test_day60_delivery_recovery_logic.py test_day60_runtime_schema_contract.py day48_alembic/versions/0009_day60_delivery_runtime.py day48_alembic/versions/0010_day60_runtime_schema.py day48_alembic/versions/0011_day60_lease_realign.py day48_alembic/versions/0012_day60_repair_audit_attestation.py
#    python3 -m pytest -q test_day60_delivery_recovery_logic.py test_day60_runtime_schema_contract.py
#
# 2) INTEGRATION_RUNTIME (OPT-IN; disposable local PostgreSQL + Redis, e.g. `--rm` Docker) —
#    needs the FULL runtime stack below. Driver split (matches the runbook):
#      * the FastAPI app + Worker use an ASYNC/sync PostgreSQL URL -> asyncpg + greenlet
#        (async bridge) and psycopg2 (sync Alembic driver, `postgresql://`).
#      * the Relay/Worker use a real Redis broker via Celery.
#    The disposable database/broker URLs and credentials are supplied via env vars at run
#    time and are NEVER committed to the repository. This local migration/runtime is
#    INSTRUCTIONAL; it is NOT a production zero-downtime plan.
fastapi==0.110.0
uvicorn==0.29.0
sqlalchemy==2.0.29
asyncpg==0.29.0
greenlet==3.0.3
psycopg2-binary==2.9.9
alembic==1.13.1
celery==5.3.6
redis==5.0.3
pydantic==2.5.0
pytest==7.4.3
